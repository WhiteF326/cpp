#include <bits/stdc++.h>
using namespace std;

#define fs(n) fixed << setprecision(n);
using ll = long long;

int main(){
  double a, b; cin >> a >> b;
  cout << fixed << setprecision(6) << (a * (b / 100.0)) << endl;
}